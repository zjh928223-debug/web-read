(function () {
  'use strict';

  // Placeholder — heavier logic extracted in Phase 4 (rendering rewrite)
  window.__transcriptStore = {
    // words, segments, wordStarts managed by app.js until Phase 4
  };
})();
